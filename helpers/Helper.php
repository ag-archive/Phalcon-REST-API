<?php

namespace api;

class Helper
{
    /*
     * checkKeys($request, ['email','password']);
     */
    public function checkKeys($request, $keys)
    {
        foreach ($keys as $key){
//            $key
        }
    }

    /*
     * check wrond_pasword_count and wrong_password_date and sets delay
     */
    public static function check_delay($data){

    }
}